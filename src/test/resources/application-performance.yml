spring:

  datasource:
    url: jdbc:mysql://localhost:3306/reviewmate
    username: root
    password: code3677z
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        #        show-sql: true
        #        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 30MB

  session:
    store-type: jdbc

#logging.level:
#  org.hibernate.SQL: DEBUG